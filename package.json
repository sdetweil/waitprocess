{
  "name": "waitprocess",
  "version": "1.0.0",
  "description": "library to wait for running or stopping process",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["wait for process to start or end"],
  "author": "sam detweiler",
  "license": "ISC",
  "dependencies": {
    "child_process": "^1.0.2"
  }
}
